<form method="post" action="{{ path('egoiliarra_delete', {'id': egoiliarra.id}) }}" onsubmit="return confirm('Ziur zaude ezabatu nahi duzula?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ egoiliarra.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash-alt"></i> Ezabatu</button>
</form>
