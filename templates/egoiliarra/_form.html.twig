{{ form_start(form) }}
    {{ form_widget(form) }}


    <hr>

    <ul class="list-inline">
        <li class="list-inline-item">
            <button class="btn btn-outline-primary"><i class="far fa-save"></i> {{ button_label|default('Gorde') }}</button>
        </li>
        <li class="list-inline-item">
            <a class="btn btn-outline-secondary" href="{{ path('egoiliarra_index') }}"><i class="far fa-arrow-alt-circle-left"></i> Itzuli zerrenda</a>
        </li>
        <li class="list-inline-item">
            {{ include('egoiliarra/_delete_form.html.twig') }}
        </li>
    </ul>

{{ form_end(form) }}
